{
  "family": "demo-web-td",
  "networkMode": "bridge",
  "cpu": "256",
  "memory": "512",
  "requiresCompatibilities": ["EC2"],
  "executionRoleArn": "arn:aws:iam::872926860633:role/demo-ecs-task-exec",
  "taskRoleArn": "arn:aws:iam::872926860633:role/demo-ecs-task-role",
  "containerDefinitions": [
    {
      "name": "demo-web",
      "image": "public.ecr.aws/nginx/nginx:alpine",
      "portMappings": [
        { "containerPort": 3000, "hostPort": 3000, "protocol": "tcp" }
      ],
      "essential": true,
      "logConfiguration": {
        "logDriver": "awslogs",
        "options": {
          "awslogs-group": "/ecs/demo-web",
          "awslogs-region": "eu-north-1",
          "awslogs-stream-prefix": "ecs"
        }
      },
      "healthCheck": {
        "retries": 3,
        "command": ["CMD-SHELL", "curl -sf http://localhost:3000/health || exit 1"],
        "timeout": 5,
        "interval": 30,
        "startPeriod": 10
      }
    }
  ]
}
